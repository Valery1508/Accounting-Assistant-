package sample.view.table;

import javafx.scene.control.TableColumn;
import javafx.scene.control.TableView;

public class DeliveryNoteTable {

    /*private final TableView table = new TableView();

    public void createTable() {
        TableColumn firstCol = new TableColumn("Code");
        TableColumn secondCol = new TableColumn("Name");
        TableColumn thirdCol = new TableColumn("Category");
        table.getColumns().addAll(firstCol, secondCol, thirdCol);
    }*/
}
